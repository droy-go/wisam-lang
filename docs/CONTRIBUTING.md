# 🤝 دليل المساهمة في لغة وسام

> **شكراً لاهتمامك بالمساهمة في لغة وسام! 🌟**

هذا الدليل سيساعدك على المساهمة في المشروع بفعالية.

---

## 📋 جدول المحتويات

1. [كيفية المساهمة](#-كيفية-المساهمة)
2. [إعداد بيئة التطوير](#-إعداد-بيئة-التطوير)
3. [هيكل المشروع](#-هيكل-المشروع)
4. [معايير الكود](#-معايير-الكود)
5. [إرسال Pull Request](#-إرسال-pull-request)
6. [الإبلاغ عن الأخطاء](#-الإبلاغ-عن-الأخطاء)

---

## 🚀 كيفية المساهمة

### أنواع المساهمات المطلوبة

| النوع | الوصف | المهارات المطلوبة |
|-------|-------|-------------------|
| 🐛 **إصلاح أخطاء** | إصلاح مشاكل في الكود | C، تصحيح الأخطاء |
| ✨ **ميزات جديدة** | إضافة وظائف جديدة | C، تصميم لغات |
| 📖 **توثيق** | تحسين الملفات التوثيقية | عربي/إنجليزي، Markdown |
| 🧪 **اختبارات** | كتابة اختبارات | C، اختبار البرمجيات |
| 🎨 **تصميم** | تحسين الشعار والواجهة | تصميم جرافيكي |
| 🌐 **ترجمة** | ترجمة التوثيق | لغات متعددة |

---

## ⚙️ إعداد بيئة التطوير

### المتطلبات

```bash
# نظام التشغيل
- Linux (Ubuntu 20.04+) أو macOS

# الأدوات المطلوبة
- GCC 9+ أو Clang 10+
- Make
- Git
- GDB (للتصحيح)
- Valgrind (لفحص الذاكرة)
```

### خطوات الإعداد

```bash
# 1. Fork المستودع
# اذهب إلى https://github.com/wisam-lang/wisam واضغط Fork

# 2. استنساخ المستودع
git clone https://github.com/your-username/wisam.git
cd wisam

# 3. إضافة الـ upstream
git remote add upstream https://github.com/wisam-lang/wisam.git

# 4. البناء
make

# 5. تشغيل الاختبارات
make test

# 6. التحقق من التثبيت
./wisam --version
```

### إعداد VS Code

```json
// .vscode/settings.json
{
    "C_Cpp.default.includePath": ["${workspaceFolder}/include"],
    "C_Cpp.default.compilerPath": "/usr/bin/gcc",
    "C_Cpp.default.cStandard": "c11",
    "files.associations": {
        "*.wsm": "wisam"
    }
}
```

---

## 📁 هيكل المشروع

```
wisam/
├── include/           # ملفات الترويسة (.h)
│   └── wisam.h       # الترويسة الرئيسية
│
├── src/              # شفرة المصدر (.c)
│   ├── main.c        # نقطة الدخول
│   ├── lexer.c       # محلل الرموز
│   ├── parser.c      # محلل النحو
│   ├── interpreter.c # المفسر
│   └── lib_*.c       # المكتبات
│
├── examples/         # أمثلة
│   ├── minimal.wsm
│   └── demo.wsm
│
├── docs/             # التوثيق
│   ├── CONCEPTS.md
│   ├── ARCHITECTURE.md
│   └── CONTRIBUTING.md
│
├── tests/            # الاختبارات
│   └── test_*.c
│
├── Makefile          # ملف البناء
└── README.md         # الملف الرئيسي
```

---

## 📝 معايير الكود

### تنسيق C

```c
// ✅ صحيح
int calculate_sum(int a, int b) {
    if (a < 0 || b < 0) {
        return -1;
    }
    
    int result = a + b;
    return result;
}

// ❌ خطأ
int calculateSum(int a,int b)
{
if(a<0||b<0)return -1;
int result=a+b;
return result;}
```

### التعليقات

```c
// ✅ تعليق عربي واضح
// حساب مجموع رقمين مع التحقق من الصحة
int calculate_sum(int a, int b) {
    // التحقق من الأرقام السالبة
    if (a < 0 || b < 0) {
        return -1;  // خطأ: رقم سالب
    }
    
    return a + b;
}

// ✅ تعليقات توثيقية للدوال
/**
 * تقوم بحساب مجموع رقمين
 * @param a الرقم الأول
 * @param b الرقم الثاني
 * @return المجموع، أو -1 في حالة الخطأ
 */
int calculate_sum(int a, int b);
```

### تسمية المتغيرات

```c
// ✅ أسماء واضحة بالعربية أو الإنجليزية
int student_count;
char *student_name;
double average_grade;

// أو بالعربية (للتناسق مع اللغة)
int عدد_الطلاب;
char *اسم_الطالب;

// ❌ أسماء غير واضحة
int n;
char *s;
double x;
```

### إدارة الذاكرة

```c
// ✅ تخصيص وتحرير صحيح
char *create_string(const char *source) {
    char *result = malloc(strlen(source) + 1);
    if (!result) {
        return NULL;  // فشل التخصيص
    }
    
    strcpy(result, source);
    return result;
}

void use_string() {
    char *str = create_string("مرحبا");
    if (str) {
        // استخدام str
        free(str);  // تحرير الذاكرة
    }
}
```

---

## 🔧 إرسال Pull Request

### خطوات العمل

```bash
# 1. التأكد من أن الـ main محدث
 git checkout main
 git pull upstream main

# 2. إنشاء فرع جديد
 git checkout -b feature/اسم-الميزة
# أو
 git checkout -b fix/وصف-الإصلاح

# 3. إجراء التغييرات
# ... تعديل الملفات ...

# 4. اختبار التغييرات
 make clean
 make
 make test

# 5. commit
 git add .
 git commit -m "نوع: الوصف

التفاصيل الكاملة للتغيير

Fixes #123"

# 6. push
 git push origin feature/اسم-الميزة

# 7. فتح Pull Request على GitHub
```

### نموذج رسالة Commit

```
نوع: العنوان المختصر (50 حرفاً كحد أقصى)

الوصف التفصيلي للتغيير. اشرح لماذا تم
هذا التغيير وكيف يعمل. يمكن كتابة
عدة أسطر إذا لزم الأمر.

- نقطة تفصيلية 1
- نقطة تفصيلية 2

Fixes #123
Related to #456
```

### أنواع Commit

| النوع | الوصف | مثال |
|-------|-------|------|
| `feat` | ميزة جديدة | `feat: إضافة دالة جديدة للنصوص` |
| `fix` | إصلاح خطأ | `fix: تصحيح تسرب ذاكرة` |
| `docs` | توثيق | `docs: تحديث README` |
| `test` | اختبارات | `test: إضافة اختبارات للمفسر` |
| `refactor` | إعادة هيكلة | `refactor: تحسين أداء الليكسر` |
| `perf` | أداء | `perf: تحسين سرعة التنفيذ` |

---

## 🐛 الإبلاغ عن الأخطاء

### نموذج الإبلاغ

```markdown
**وصف الخطأ**
وصف واضح ومختصر للمشكلة.

**للتكرار**
خطوات إعادة إنتاج الخطأ:
1. اذهب إلى '...'
2. انقر على '...'
3. انظر إلى الخطأ

**السلوك المتوقع**
ما الذي توقعت حدوثه؟

**السلوك الفعلي**
ما الذي حدث فعلاً؟

**لقطات الشاشة**
إن وجدت.

**بيئة التشغيل**
- نظام التشغيل: [مثال: Ubuntu 20.04]
- الإصدار: [مثال: 1.0.0]
- المترجم: [مثال: GCC 9.3]

**كود للتكرار**
```wisam
# ضع الكود هنا
```
```

### التصنيفات

| الوسم | المعنى |
|-------|--------|
| `bug` | خطأ في الكود |
| `enhancement` | تحسين أو ميزة جديدة |
| `documentation` | مشكلة في التوثيق |
| `good first issue` | مناسب للمبتدئين |
| `help wanted` | يحتاج مساعدة |

---

## 🧪 كتابة الاختبارات

### اختبارات الوحدة

```c
// tests/test_lexer.c
#include "test_framework.h"
#include "wisam.h"

void test_arabic_keyword() {
    Lexer *lexer = lexer_create("ليكن");
    int count;
    Token *tokens = lexer_tokenize(lexer, &count);
    
    assert(tokens[0].type == TOKEN_LET);
    assert_string_equals(tokens[0].value, "ليكن");
    
    lexer_destroy(lexer);
    free(tokens);
}

void test_number_token() {
    Lexer *lexer = lexer_create("123");
    int count;
    Token *tokens = lexer_tokenize(lexer, &count);
    
    assert(tokens[0].type == TOKEN_NUMBER);
    assert_string_equals(tokens[0].value, "123");
    
    lexer_destroy(lexer);
    free(tokens);
}

int main() {
    run_test(test_arabic_keyword);
    run_test(test_number_token);
    
    print_results();
    return 0;
}
```

### اختبارات التكامل

```wisam
# tests/integration/test_math.wsm

# اختبار العمليات الحسابية
ليكن أ = 10
ليكن ب = 5

assert(أ + ب == 15)
assert(أ - ب == 5)
assert(أ * ب == 50)
assert(أ / ب == 2)

# اختبار الدوال
dالة ضعف تأخذ س
    أعد س * 2
انتهى

assert(ضعف(5) == 10)
assert(ضعف(10) == 20)

اكتب "✓ جميع اختبارات الرياضيات ناجحة"
```

---

## 🎨 إضافة مكتبة جديدة

### الخطوات

```c
// 1. إنشاء ملف src/lib_mylibrary.c

#include "wisam.h"

// 2. تنفيذ الدوال
Value lib_mylibrary_function(Value *args, int arg_count) {
    // التحقق من المعاملات
    if (arg_count < 1 || args[0].type != VAL_STRING) {
        fprintf(stderr, "خطأ: الدالة تتطلب نصاً\n");
        return value_create_null();
    }
    
    // المنطق
    char *input = args[0].as.string;
    char *result = process(input);
    
    return value_create_string(result);
}

// 3. إضافة التصريح في wisam.h
Value lib_mylibrary_function(Value *args, int arg_count);

// 4. تسجيل المكتبة في interpreter.c
void register_libraries() {
    // ... المكتبات الأخرى
    register_library("مكتبتي", mylibrary_functions);
}

// 5. كتابة الاختبارات
// tests/test_mylibrary.c

// 6. تحديث التوثيق
// docs/libraries/mylibrary.md
```

---

## 📞 التواصل

### قنوات التواصل

| القناة | الرابط | الاستخدام |
|--------|--------|-----------|
| Discord | [discord.gg/wisam](https://discord.gg/wisam) | مناقشات عامة |
| Twitter | [@WisamLang](https://twitter.com/WisamLang) | إعلانات |
| Email | [droy.ree@gmail.com](mailto:droy.ree@gmail.com) | خاص |
| Issues | [GitHub Issues](https://github.com/wisam-lang/wisam/issues) | أخطاء وميزات |

### ساعات العمل

نحن متواجدون للإجابة على استفساراتكم:
- **الأحد - الخميس:** 9:00 ص - 6:00 م (توقيت مكة)
- **الجمعة - السبت:** إجازة

---

<div align="center">

## 🌟 شكراً للمساهمة! 🌟

كل مساهمة، مهما كانت صغيرة، تجعل **وسام** أفضل!

**[العودة للأعلى ⬆️](#-جدول-المحتويات)**

</div>
