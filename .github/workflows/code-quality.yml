name: Code Quality

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

jobs:
  lint:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Install cppcheck
      run: sudo apt-get install -y cppcheck
    
    - name: Run cppcheck
      run: |
        cppcheck --enable=all --suppress=missingIncludeSystem \
          --error-exitcode=1 \
          src/ stdlib/ 2>&1 | tee cppcheck-report.txt
    
    - name: Upload cppcheck report
      uses: actions/upload-artifact@v7
      if: always()
      with:
        name: cppcheck-report
        path: cppcheck-report.txt

  static-analysis:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Install Clang Static Analyzer
      run: sudo apt-get install -y clang-tools
    
    - name: Run scan-build
      run: |
        scan-build --status-bugs make clean all 2>&1 | tee scan-build-report.txt
    
    - name: Upload scan-build report
      uses: actions/upload-artifact@v7
      if: always()
      with:
        name: scan-build-report
        path: scan-build-report.txt

  memory-check:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Install valgrind
      run: sudo apt-get install -y valgrind
    
    - name: Build with debug symbols
      run: make clean && CFLAGS="-g -O0" make
    
    - name: Run valgrind
      run: |
        valgrind --leak-check=full --show-leak-kinds=all \
          --track-origins=yes --verbose \
          ./wisam examples/hello.wsm 2>&1 | tee valgrind-report.txt || true
    
    - name: Upload valgrind report
      uses: actions/upload-artifact@v7
      if: always()
      with:
        name: valgrind-report
        path: valgrind-report.txt
