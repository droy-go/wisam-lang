# 📚 دليل المكتبات - لغة وسام

## مقدمة

لغة وسام تأتي مع مجموعة غنية من المكتبات تغطي مختلف المجالات:

- معالجة النصوص والبيانات
- العمليات الرياضية والإحصائية
- التعامل مع الملفات والنظام
- الذكاء الاصطناعي والتعلم الآلي
- الشبكات والإنترنت
- واجهات المستخدم

---

## المكتبات الأساسية

### 1. مكتبة النصوص (`نصوص`)

مكتبة شاملة لمعالجة النصوص باللغة العربية والإنجليزية.

#### الدوال المتاحة

| الدالة | الوصف | المثال |
|--------|-------|--------|
| `حوّل_إلى_كبير(نص)` | تحويل النص لحروف كبيرة | `نصوص.حوّل_إلى_كبير("hello")` → `"HELLO"` |
| `حوّل_إلى_صغير(نص)` | تحويل النص لحروف صغيرة | `نصوص.حوّل_إلى_صغير("HELLO")` → `"hello"` |
| `الطول(نص)` | طول النص | `نصوص.الطول("مرحبا")` → `6` |
| `جزء_من_نص(نص، بداية، طول)` | استخراج جزء من النص | `نصوص.جزء_من_نص("مرحبا"، 0، 3)` → `"مرح"` |
| `استبدل(نص، قديم، جديد)` | استبدال نص بآخر | `نصوص.استبدل("مرحبا"، "حب"، "مل")` → `"مرملا"` |
| `قسّم(نص، فاصل)` | تقسيم النص | `نصوص.قسّم("أ-ب-ت"، "-")` → `["أ"، "ب"، "ت"]` |
| `قص(نص)` | إزالة المسافات | `نصوص.قص("  مرحبا  ")` → `"مرحبا"` |
| `يبدأ_بـ(نص، بادئة)` | التحقق من البداية | `نصوص.يبدأ_بـ("مرحبا"، "مر")` → `صحيح` |
| `ينتهي_بـ(نص، لاحقة)` | التحقق من النهاية | `نصوص.ينتهي_بـ("مرحبا"، "با")` → `صحيح` |
| `يحتوي_على(نص، نص_فرعي)` | التحقق من الوجود | `نصوص.يحتوي_على("مرحبا"، "حب")` → `صحيح` |
| `موضع(نص، نص_للبحث)` | البحث عن موضع | `نصوص.موضع("مرحبا"، "حب")` → `2` |
| `كرّر(نص، عدد)` | تكرار النص | `نصوص.كرّر("أ"، 3)` → `"أأأ"` |
| `اقلب(نص)` | عكس النص | `نصوص.اقلب("مرحبا")` → `"ابحرم"` |
| `صيّغ(قالب، ...)` | تنسيق النص | `نصوص.صيّغ("مرحبا {0}"، "أحمد")` → `"مرحبا أحمد"` |

#### مثال
```wisam
استورد نصوص

ليكن نص = "  مرحبا بك في وسام  "

# إزالة المسافات
ليكن مقصوص = نصوص.قص(نص)

# تحويل لحروف كبيرة
ليكن كبير = نصوص.حوّل_إلى_كبير(مقصوص)

# تقسيم
ليكن كلمات = نصوص.قسّم(مقصوص، " ")

# استبدال
ليكن معدل = نصوص.استبدل(نص، "وسام"، "العالم")

اكتب مقصوص
اكتب كبير
اكتب كلمات
اكتب معدل
```

---

### 2. مكتبة الرياضيات (`رياضيات`)

مكتبة شاملة للعمليات الرياضية والإحصائية.

#### الدوال الأساسية

| الدالة | الوصف | المثال |
|--------|-------|--------|
| `جذر(رقم)` | الجذر التربيعي | `رياضيات.جذر(16)` → `4` |
| `أس(أساس، أس)` | الأس | `رياضيات.أس(2، 3)` → `8` |
| `جيب(زاوية)` | جيب الزاوية | `رياضيات.جيب(0)` → `0` |
| `جيب_التام(زاوية)` | جيب التام | `رياضيات.جيب_التام(0)` → `1` |
| `ظل(زاوية)` | الظل | `رياضيات.ظل(0)` → `0` |
| `لوغاريتم(رقم)` | اللوغاريتم الطبيعي | `رياضيات.لوغاريتم(2.718)` → `1` |
| `لوغاريتم10(رقم)` | اللوغاريتم العشري | `رياضيات.لوغاريتم10(100)` → `2` |
| `أس_اللوغاريتم(رقم)` | الأس | `رياضيات.أس_اللوغاريتم(1)` → `2.718` |
| `أرض(رقم)` | التقريب لأسفل | `رياضيات.أرض(3.7)` → `3` |
| `سقف(رقم)` | التقريب لأعلى | `رياضيات.سقف(3.2)` → `4` |
| `قريب(رقم)` | التقريب | `رياضيات.قريب(3.5)` → `4` |
| `مطلق(رقم)` | القيمة المطلقة | `رياضيات.مطلق(-5)` → `5` |

#### الدوال الإحصائية

| الدالة | الوصف | المثال |
|--------|-------|--------|
| `مجموع(مصفوفة)` | مجموع العناصر | `رياضيات.مجموع([1، 2، 3])` → `6` |
| `متوسط(مصفوفة)` | المتوسط الحسابي | `رياضيات.متوسط([1، 2، 3])` → `2` |
| `وسيط(مصفوفة)` | الوسيط | `رياضيات.وسيط([1، 2، 3])` → `2` |
| `منوال(مصفوفة)` | المنوال | `رياضيات.منوال([1، 2، 2، 3])` → `2` |
| `انحراف_معياري(مصفوفة)` | الانحراف المعياري | `رياضيات.انحراف_معياري([1، 2، 3])` |
| `تباين(مصفوفة)` | التباين | `رياضيات.تباين([1، 2، 3])` |
| `أصغر(مصفوفة)` | أصغر قيمة | `رياضيات.أصغر([1، 2، 3])` → `1` |
| `أكبر(مصفوفة)` | أكبر قيمة | `رياضيات.أكبر([1، 2، 3])` → `3` |

#### الثوابت

| الثابت | القيمة |
|--------|--------|
| `باي` | 3.14159265359 |
| `هـ` | 2.71828182846 |
| `ذهبي` | 1.61803398875 |

#### مثال
```wisam
استورد رياضيات

# عمليات أساسية
اكتب رياضيات.جذر(16)           # 4
اكتب رياضيات.أس(2، 3)          # 8

# إحصائيات
ليكن أرقام = [1، 2، 3، 4، 5]
اكتب رياضيات.مجموع(أرقام)      # 15
اكتب رياضيات.متوسط(أرقام)      # 3

# ثوابت
اكتب رياضيات.باي               # 3.14159...
```

---

### 3. مكتبة الزمن (`زمن`)

مكتبة للتعامل مع الوقت والتاريخ.

#### الدوال

| الدالة | الوصف | المثال |
|--------|-------|--------|
| `الآن()` | الوقت الحالي | `زمن.الآن()` |
| `صيّغ(وقت، نمط)` | تنسيق الوقت | `زمن.صيّغ(الآن، "YYYY-MM-DD")` |
| `نم(ثواني)` | تأخير | `زمن.نم(2)` |
| `السنة()` | السنة الحالية | `زمن.السنة()` → `2025` |
| `الشهر()` | الشهر الحالي | `زمن.الشهر()` → `2` |
| `اليوم()` | اليوم الحالي | `زمن.اليوم()` → `25` |
| `الساعة()` | الساعة الحالية | `زمن.الساعة()` |
| `الدقيقة()` | الدقيقة الحالية | `زمن.الدقيقة()` |
| `الثانية()` | الثانية الحالية | `زمن.الثانية()` |
| `طابع_زمني()` | الطابع الزمني | `زمن.طابع_زمني()` |

#### مثال
```wisam
استورد زمن

# الوقت الحالي
ليكن الآن = زمن.الآن()
اكتب "الوقت الحالي: {الآن}"

# تنسيق
ليكن منسق = زمن.صيّغ(الآن، "YYYY/MM/DD")
اكتب "التاريخ: {منسق}"

# تأخير
زمن.نم(1)  # انتظر ثانية واحدة
```

---

### 4. مكتبة القوائم (`قوائم`)

مكتبة للتعامل مع المصفوفات والقوائم.

#### الدوال

| الدالة | الوصف | المثال |
|--------|-------|--------|
| `أضف(قائمة، عنصر)` | إضافة عنصر | `قوائم.أضف(قائمة، 5)` |
| `أزل(قائمة)` | إزالة آخر عنصر | `قوائم.أزل(قائمة)` |
| `أدخل(قائمة، عنصر)` | إضافة في البداية | `قوائم.أدخل(قائمة، 1)` |
| `أزل_أول(قائمة)` | إزالة أول عنصر | `قوائم.أزل_أول(قائمة)` |
| `الطول(قائمة)` | طول القائمة | `قوائم.الطول(قائمة)` → `5` |
| `احصل(قائمة، فهرس)` | الحصول على عنصر | `قوائم.احصل(قائمة، 0)` |
| `اضبط(قائمة، فهرس، قيمة)` | تعديل عنصر | `قوائم.اضبط(قائمة، 0، 10)` |
| `أزل_عند(قائمة، فهرس)` | إزالة عند فهرس | `قوائم.أزل_عند(قائمة، 2)` |
| `موضع(قائمة، عنصر)` | البحث عن عنصر | `قوائم.موضع(قائمة، 5)` |
| `يحتوي(قائمة، عنصر)` | التحقق من الوجود | `قوائم.يحتوي(قائمة، 5)` → `صحيح` |
| `رتّب(قائمة)` | ترتيب القائمة | `قوائم.رتّب(قائمة)` |
| `اقلب(قائمة)` | عكس القائمة | `قوائم.اقلب(قائمة)` |
| `اجمع(قائمة، فاصل)` | دمج القائمة | `قوائم.اجمع(["أ"، "ب"]， "-")` → `"أ-ب"` |

#### مثال
```wisam
استورد قوائم

ليكن أرقام = [3، 1، 4، 1، 5]

# إضافة
قوائم.أضف(أرقام، 9)

# ترتيب
قوائم.رتّب(أرقام)

# عكس
قوائم.اقلب(أرقام)

# طول
ليكن طول = قوائم.الطول(أرقام)

اكتب أرقام
اكتب "الطول: {طول}"
```

---

### 5. مكتبة الملفات (`ملفات`)

مكتبة للتعامل مع الملفات والمجلدات.

#### الدوال

| الدالة | الوصف | المثال |
|--------|-------|--------|
| `اقرأ(مسار)` | قراءة ملف | `ملفات.اقرأ("data.txt")` |
| `اكتب(مسار، محتوى)` | كتابة ملف | `ملفات.اكتب("out.txt"، "مرحبا")` |
| `ألحق(مسار، محتوى)` | إلحاق بملف | `ملفات.ألحق("log.txt"، "سجل")` |
| `موجود(مسار)` | التحقق من الوجود | `ملفات.موجود("file.txt")` → `صحيح` |
| `احذف(مسار)` | حذف ملف | `ملفات.احذف("old.txt")` |
| `انسخ(مصدر، هدف)` | نسخ ملف | `ملفات.انسخ("a.txt"، "b.txt")` |
| `انقل(مصدر، هدف)` | نقل ملف | `ملفات.انقل("a.txt"، "b.txt")` |
| `الحجم(مسار)` | حجم الملف | `ملفات.الحجم("file.txt")` |
| `أنشئ_مجلد(مسار)` | إنشاء مجلد | `ملفات.أنشئ_مجلد("new_folder")` |
| `احذف_مجلد(مسار)` | حذف مجلد | `ملفات.احذف_مجلد("old_folder")` |
| `المحتويات(مسار)` | قائمة الملفات | `ملفات.المحتويات("./")` |
| `هل_مجلد(مسار)` | هل مجلد؟ | `ملفات.هل_مجلد("folder")` → `صحيح` |
| `هل_ملف(مسار)` | هل ملف؟ | `ملفات.هل_ملف("file.txt")` → `صحيح` |

#### مثال
```wisam
استورد ملفات

# قراءة
ليكن محتوى = ملفات.اقرأ("data.txt")

# كتابة
ملفات.اكتب("output.txt"، "مرحبا بك في وسام")

# إنشاء مجلد
ملفات.أنشئ_مجلد("مجلد_جديد")

# قائمة الملفات
ليكن ملفات = ملفات.المحتويات("./")

اكتب ملفات
```

---

## المكتبات المتقدمة

### 6. مكتبة JSON (`json`)

مكتبة للتعامل مع بيانات JSON.

#### الدوال

| الدالة | الوصف | المثال |
|--------|-------|--------|
| `حلّل(نص)` | تحليل JSON | `json.حلّل('{"name": "أحمد"}')` |
| `سلّسل(كائن)` | تحويل لـ JSON | `json.سلّسل(كائن)` |
| `صالح(نص)` | التحقق من الصلاحية | `json.صالح(نص)` → `صحيح` |
| `احصل(كائن، مفتاح)` | الحصول على قيمة | `json.احصل(كائن، "name")` |
| `اضبط(كائن، مفتاح، قيمة)` | تعيين قيمة | `json.اضبط(كائن، "age"، 25)` |
| `احذف(كائن، مفتاح)` | حذف مفتاح | `json.احذف(كائن، "old")` |
| `المفاتيح(كائن)` | قائمة المفاتيح | `json.المفاتيح(كائن)` |
| `القيم(كائن)` | قائمة القيم | `json.القيم(كائن)` |

#### مثال
```wisam
استورد json

# تحليل
ليكن بيانات = json.حلّل('{"name": "أحمد"، "age": 25}')

# الحصول على قيمة
ليكن الاسم = json.احصل(بيانات، "name")

# تعيين قيمة
json.اضبط(بيانات، "city"، "الرياض")

# تحويل لنص
ليكن نص = json.سلّسل(بيانات)

اكتب الاسم
اكتب نص
```

---

### 7. مكتبة التشفير (`تشفير`)

مكتبة للتشفير والتجزئة.

#### الدوال

| الدالة | الوصف | المثال |
|--------|-------|--------|
| `md5(نص)` | تجزئة MD5 | `تشفير.md5("مرحبا")` |
| `sha1(نص)` | تجزئة SHA1 | `تشفير.sha1("مرحبا")` |
| `sha256(نص)` | تجزئة SHA256 | `تشفير.sha256("مرحبا")` |
| `sha512(نص)` | تجزئة SHA512 | `تشفير.sha512("مرحبا")` |
| `تشفير64(نص)` | تشفير Base64 | `تشفير.تشفير64("مرحبا")` |
| `فك64(نص)` | فك Base64 | `تشفير.فك64(مشفر)` |
| `معرّف_فريد()` | UUID | `تشفير.معرّف_فريد()` |
| `بايتات_عشوائية(عدد)` | بايتات عشوائية | `تشفير.بايتات_عشوائية(16)` |

#### مثال
```wisam
استورد تشفير

# تجزئة
ليكن hash = تشفير.sha256("مرحبا")

# Base64
ليكن مشفر = تشفير.تشفير64("مرحبا")
ليكن مفكوك = تشفير.فك64(مشفر)

# UUID
ليكن id = تشفير.معرّف_فريد()

اكتب hash
اكتب مشفر
اكتب id
```

---

### 8. مكتبة النظام (`نظام`)

مكتبة للعمليات النظامية.

#### الدوال

| الدالة | الوصف | المثال |
|--------|-------|--------|
| `نفّذ(أمر)` | تنفيذ أمر | `نظام.نفّذ("ls -la")` |
| `المتغير(اسم)` | قراءة متغير بيئة | `نظام.المتغير("HOME")` |
| `اضبط_متغير(اسم، قيمة)` | تعيين متغير | `نظام.اضبط_متغير("X"، "1")` |
| `المعاملات()` | معاملات سطر الأوامر | `نظام.المعاملات()` |
| `اخرج(كود)` | إنهاء البرنامج | `نظام.اخرج(0)` |
| `معرّف_العملية()` | PID | `نظام.معرّف_العملية()` |
| `المجلد_الحالي()` | المجلد الحالي | `نظام.المجلد_الحالي()` |
| `غيّر_المجلد(مسار)` | تغيير المجلد | `نظام.غيّر_المجلد("/tmp")` |
| `النظام()` | اسم النظام | `نظام.النظام()` → `"Linux"` |
| `المعمارية()` | المعمارية | `نظام.المعمارية()` → `"x86_64"` |
| `عدد_المعالجات()` | عدد المعالجات | `نظام.عدد_المعالجات()` |
| `الذاكرة()` | إجمالي الذاكرة | `نظام.الذاكرة()` |
| `الذاكرة_الحرة()` | الذاكرة الحرة | `نظام.الذاكرة_الحرة()` |

#### مثال
```wisam
استورد نظام

# تنفيذ أمر
ليكن ناتج = نظام.نفّذ("echo مرحبا")

# معلومات النظام
اكتب نظام.النظام()
اكتب نظام.المعمارية()

# الذاكرة
اكتب "الذاكرة: {نظام.الذاكرة()}"
```

---

## المكتبات الخاصة

### 9. مكتبة الذكاء الاصطناعي (`ذكاء`)

مكتبة للذكاء الاصطناعي والتعلم الآلي.

#### الدوال

| الدالة | الوصف | المثال |
|--------|-------|--------|
| `أنشئ_عقل(اسم)` | إنشاء عقل | `ذكاء.أنشئ_عقل("مساعد")` |
| `علّم(عقل، مدخلات، مخرجات)` | تدريب | `ذكاء.علّم(عقل، "مرحبا"، "أهلاً")` |
| `اسأل(عقل، سؤال)` | سؤال | `ذكاء.اسأل(عقل، "مرحبا")` |
| `احفظ(عقل، مسار)` | حفظ | `ذكاء.احفظ(عقل، "brain.dat")` |
| `حمّل(عقل، مسار)` | تحميل | `ذكاء.حمّل(عقل، "brain.dat")` |
| `أنشئ_شبكة(اسم، طبقات)` | إنشاء شبكة | `ذكاء.أنشئ_شبكة("شبكة1"، [3، 5، 1])` |
| `درّب(شبكة، بيانات)` | تدريب | `ذكاء.درّب(شبكة، بيانات)` |
| `تنبأ(شبكة، مدخل)` | تنبؤ | `ذكاء.تنبأ(شبكة، [0.5، 0.3، 0.2])` |

#### مثال
```wisam
استورد ذكاء

# إنشاء عقل
أنشئ عقل باسم مساعد

# تدريب
مساعد.تعلّم
    "مرحبا" = "أهلاً بك! 👋"
    "كيف حالك" = "أنا بخير، شكراً!"
    "ما اسمك" = "أنا وسام"
انتهى

# استخدام
ليكن رد = مساعد.اسأل("مرحبا")
اكتب رد
```

---

### 10. مكتبة الشبكة (`شبكة`)

مكتبة للشبكات والإنترنت.

#### الدوال

| الدالة | الوصف | المثال |
|--------|-------|--------|
| `احصل(رابط)` | GET | `شبكة.احصل("https://api.example.com")` |
| `أرسل(رابط، بيانات)` | POST | `شبكة.أرسل(رابط، بيانات)` |
| `عدّل(رابط، بيانات)` | PUT | `شبكة.عدّل(رابط، بيانات)` |
| `احذف(رابط)` | DELETE | `شبكة.احذف(رابط)` |
| `حمّل(رابط، مسار)` | تحميل | `شبكة.حمّل(رابط، "file.zip")` |
| `أنشئ_خادم(منفذ)` | إنشاء خادم | `شبكة.أنشئ_خادم(8080)` |
| `ترميز_رابط(نص)` | ترميز URL | `شبكة.ترميز_رابط("hello world")` |
| `فك_رابط(نص)` | فك URL | `شبكة.فك_رابط(مشفر)` |

#### مثال
```wisam
استورد شبكة

# GET
ليكن استجابة = شبكة.احصل("https://api.example.com/data")

# POST
ليكن بيانات = '{"name": "أحمد"}'
ليكن ناتج = شبكة.أرسل("https://api.example.com/users"، بيانات)

# تحميل
شبكة.حمّل("https://example.com/file.zip"، "file.zip")
```

---

## ملاحظات

- جميع المكتبات تدعم اللغة العربية بالكامل
- الأخطاء تظهر باللغة العربية
- المكتبات مصممة لتكون سهلة الاستخدام

---

**📖 للمزيد من التفاصيل، راجع الأمثلة في مجلد `examples/`**
