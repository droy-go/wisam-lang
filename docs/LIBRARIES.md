# 📚 مكتبات لغة وسام

> **دليل شامل لجميع المكتبات المتوفرة في لغة وسام**

---

## 📋 جدول المكتبات

| المكتبة | الوصف | الدوال |
|---------|-------|--------|
| **نصوص** | معالجة النصوص | 8 دوال |
| **زمن** | التعامل مع الوقت | 12 دالة |
| **رياضيات** | العمليات الرياضية | 25 دالة |
| **قوائم** | التعامل مع القوائم | 20 دالة |
| **ملفات** | العمليات على الملفات | 18 دالة |
| **json** | التعامل مع JSON | 6 دوال |
| **تشفير** | التشفير والتجزئة | 10 دوال |
| **تعابير** | التعابير النمطية | 10 دوال |
| **نظام** | العمليات النظامية | 20 دالة |
| **مخزن** | التخزين الدائم | 8 دوال |
| **ذكاء** | الذكاء الاصطناعي | 15 دالة |
| **وسائط** | الوسائط المتعددة | 8 دوال |
| **واجهات** | واجهات المستخدم | 12 دالة |
| **شبكة** | الشبكات والإنترنت | 10 دوال |
| **ميتا** | معلومات النظام | 12 دالة |

---

## 🔤 مكتبة النصوص

### الاستيراد
```wisam
استورد نصوص
```

### الدوال

| الدالة | الوصف | مثال |
|--------|-------|------|
| `حوّل_إلى_كبير(نص)` | تحويل لأحرف كبيرة | `نصوص.حوّل_إلى_كبير("مرحبا")` → `"مرحبا"` |
| `حوّل_إلى_صغير(نص)` | تحويل لأحرف صغيرة | `نصوص.حوّل_إلى_صغير("HELLO")` → `"hello"` |
| `الطول(نص)` | طول النص | `نصوص.الطول("مرحبا")` → `5` |
| `جزء(نص، بداية، طول)` | استخراج جزء | `نصوص.جزء("مرحبا"، 0، 3)` → `"مرح"` |
| `استبدل(نص، قديم، جديد)` | استبدال نص | `نصوص.استبدل("أحمد"، "أ"، "م")` → `"محمد"` |
| `قسّم(نص، فاصل)` | تقسيم النص | `نصوص.قسّم("أ-ب-ت"، "-")` → `["أ"، "ب"، "ت"]` |
| `قص(نص)` | إزالة المسافات | `نصوص.قص("  مرحبا  ")` → `"مرحبا"` |
| `ترجم(نص، إلى)` | ترجمة النص | `نصوص.ترجم("مرحبا"، "English")` → `"Hello"` |

---

## ⏰ مكتبة الزمن

### الاستيراد
```wisam
استورد زمن
```

### الدوال

| الدالة | الوصف | مثال |
|--------|-------|------|
| `الآن()` | الوقت الحالي | `زمن.الآن()` → `"2024-02-25 10:30:00"` |
| `السنة()` | السنة الحالية | `زمن.السنة()` → `2024` |
| `الشهر()` | الشهر الحالي | `زمن.الشهر()` → `2` |
| `اليوم()` | اليوم الحالي | `زمن.اليوم()` → `25` |
| `الساعة()` | الساعة الحالية | `زمن.الساعة()` → `10` |
| `الدقيقة()` | الدقيقة الحالية | `زمن.الدقيقة()` → `30` |
| `الثانية()` | الثانية الحالية | `زمن.الثانية()` → `0` |
| `يوم_الأسبوع()` | اسم اليوم | `زمن.يوم_الأسبوع()` → `"الأحد"` |
| `اسم_الشهر()` | اسم الشهر | `زمن.اسم_الشهر()` → `"فبراير"` |
| `تأخير(ثواني)` | تأخير التنفيذ | `زمن.تأخير(5)` |
| `تأخير_مللي(مللي)` | تأخير بالمللي | `زمن.تأخير_مللي(500)` |
| `صيّغ(نمط)` | تنسيق الوقت | `زمن.صيّغ("YYYY-MM-DD")` |

---

## 🔢 مكتبة الرياضيات

### الاستيراد
```wisam
استورد رياضيات
```

### الدوال الأساسية

| الدالة | الوصف | مثال |
|--------|-------|------|
| `قيمة_مطلقة(س)` | القيمة المطلقة | `رياضيات.قيمة_مطلقة(-5)` → `5` |
| `جذر(س)` | الجذر التربيعي | `رياضيات.جذر(16)` → `4` |
| `أس(س، ص)` | الأس | `رياضيات.أس(2، 3)` → `8` |
| `جيب(س)` | جيب الزاوية | `رياضيات.جيب(0)` → `0` |
| `جيب_تمام(س)` | جيب التمام | `رياضيات.جيب_تمام(0)` → `1` |
| `ظل(س)` | الظل | `رياضيات.ظل(0)` → `0` |
| `لوغاريتم(س)` | اللوغاريتم الطبيعي | `رياضيات.لوغاريتم(2.718)` → `1` |
| `لوغاريتم10(س)` | اللوغاريتم العشري | `رياضيات.لوغاريتم10(100)` → `2` |
| `أس_النيبير(س)` | e^x | `رياضيات.أس_النيبير(1)` → `2.718` |
| `أرضي(س)` | التقريب للأسفل | `رياضيات.أرضي(3.7)` → `3` |
| `سقف(س)` | التقريب للأعلى | `رياضيات.سقف(3.2)` → `4` |
| `تقريب(س)` | التقريب | `رياضيات.تقريب(3.5)` → `4` |

### الدوال الإحصائية

| الدالة | الوصف | مثال |
|--------|-------|------|
| `أكبر(...)` | أكبر قيمة | `رياضيات.أكبر(1، 5، 3)` → `5` |
| `أصغر(...)` | أصغر قيمة | `رياضيات.أصغر(1، 5، 3)` → `1` |
| `مجموع(قائمة)` | مجموع القائمة | `رياضيات.مجموع([1،2،3])` → `6` |
| `متوسط(قائمة)` | المتوسط | `رياضيات.متوسط([1،2،3])` → `2` |
| `وسيط(قائمة)` | الوسيط | `رياضيات.وسيط([1،2،3،4،5])` → `3` |

### الثوابت

| الثابت | القيمة |
|--------|--------|
| `رياضيات.باي` | 3.14159... |
| `رياضيات.نيبير` | 2.71828... |

---

## 📋 مكتبة القوائم

### الاستيراد
```wisam
استورد قوائم
```

### الدوال

| الدالة | الوصف | مثال |
|--------|-------|------|
| `أنشئ()` | إنشاء قائمة | `قوائم.أنشئ()` → `[]` |
| `أضف(قائمة، عنصر)` | إضافة عنصر | `قوائم.أضف([1،2]، 3)` → `[1،2،3]` |
| `أدرج(قائمة، موضع، عنصر)` | إدراج في موضع | `قوائم.أدرج([1،3]، 1، 2)` → `[1،2،3]` |
| `احذف(قائمة، موضع)` | حذف من موضع | `قوائم.احذف([1،2،3]، 1)` → `[1،3]` |
| `احصل(قائمة، موضع)` | الحصول على عنصر | `قوائم.احصل([1،2،3]، 0)` → `1` |
| `عيّن(قائمة، موضع، قيمة)` | تعيين قيمة | `قوائم.عيّن([1،2،3]، 0، 10)` |
| `الحجم(قائمة)` | حجم القائمة | `قوائم.الحجم([1،2،3])` → `3` |
| `فهرس(قائمة، عنصر)` | فهرس العنصر | `قوائم.فهرس([1،2،3]، 2)` → `1` |
| `يحتوي(قائمة، عنصر)` | التحقق من الوجود | `قوائم.يحتوي([1،2،3]، 2)` → `صحيح` |
| `اعكس(قائمة)` | عكس القائمة | `قوائم.اعكس([1،2،3])` → `[3،2،1]` |
| `انسخ(قائمة)` | نسخ القائمة | `قوائم.انسخ([1،2،3])` → `[1،2،3]` |
| `ادمج(قائمة1، قائمة2)` | دمج قائمتين | `قوائم.ادمج([1،2]، [3،4])` → `[1،2،3،4]` |
| `رتّب(قائمة)` | ترتيب القائمة | `قوائم.رتّب([3،1،2])` → `[1،2،3]` |
| `امسح(قائمة)` | مسح القائمة | `قوائم.امسح([1،2،3])` → `[]` |
| `أول(قائمة)` | أول عنصر | `قوائم.أول([1،2،3])` → `1` |
| `آخر(قائمة)` | آخر عنصر | `قوائم.آخر([1،2،3])` → `3` |
| `خذ(قائمة، n)` | أخذ n عنصر | `قوائم.خذ([1،2،3،4]، 2)` → `[1،2]` |
| `تخطى(قائمة، n)` | تخطي n عنصر | `قوائم.تخطى([1،2،3،4]، 2)` → `[3،4]` |

---

## 📁 مكتبة الملفات

### الاستيراد
```wisam
استورد ملفات
```

### الدوال

| الدالة | الوصف | مثال |
|--------|-------|------|
| `اقرأ(مسار)` | قراءة ملف | `ملفات.اقرأ("data.txt")` |
| `اكتب(مسار، محتوى)` | كتابة ملف | `ملفات.اكتب("out.txt"، "مرحبا")` |
| `ألحق(مسار، محتوى)` | إلحاق بملف | `ملفات.ألحق("log.txt"، "سطر جديد")` |
| `احذف(مسار)` | حذف ملف | `ملفات.احذف("old.txt")` |
| `موجود(مسار)` | التحقق من الوجود | `ملفات.موجود("file.txt")` |
| `الحجم(مسار)` | حجم الملف | `ملفات.الحجم("file.txt")` |
| `أنشئ_مجلد(مسار)` | إنشاء مجلد | `ملفات.أنشئ_مجلد("new_folder")` |
| `احذف_مجلد(مسار)` | حذف مجلد | `ملفات.احذف_مجلد("old_folder")` |
| `اقرأ_سطور(مسار)` | قراءة كسطور | `ملفات.اقرأ_سطور("data.txt")` |
| `اكتب_سطور(مسار، سطور)` | كتابة سطور | `ملفات.اكتب_سطور("out.txt"، ["سطر1"، "سطر2"])` |
| `انسخ(مصدر، هدف)` | نسخ ملف | `ملفات.انسخ("a.txt"، "b.txt")` |
| `انقل(مصدر، هدف)` | نقل ملف | `ملفات.انقل("a.txt"، "folder/b.txt")` |
| `محتويات(مسار)` | قائمة الملفات | `ملفات.محتويات("./")` |
| `المجلد_الحالي()` | المجلد الحالي | `ملفات.المجلد_الحالي()` |
| `غيّر_المجلد(مسار)` | تغيير المجلد | `ملفات.غيّر_المجلد("/home")` |
| `معلومات(مسار)` | معلومات الملف | `ملفات.معلومات("file.txt")` |

---

## 📦 مكتبة JSON

### الاستيراد
```wisam
استورد json
```

### الدوال

| الدالة | الوصف | مثال |
|--------|-------|------|
| `حوّل(كائن)` | تحويل لـ JSON | `json.حوّل({"اسم": "أحمد"})` |
| `حلّل(نص)` | تحليل JSON | `json.حلّل("{\"اسم\": \"أحمد\"}")` |
| `اقرأ_ملف(مسار)` | قراءة ملف JSON | `json.اقرأ_ملف("data.json")` |
| `اكتب_ملف(مسار، كائن)` | كتابة ملف JSON | `json.اكتب_ملف("out.json"، {"اسم": "أحمد"})` |
| `احصل(كائن، مفتاح)` | الحصول على قيمة | `json.احصل(كائن، "اسم")` |
| `عيّن(كائن، مفتاح، قيمة)` | تعيين قيمة | `json.عيّن(كائن، "عمر"، 25)` |

---

## 🔐 مكتبة التشفير

### الاستيراد
```wisam
استورد تشفير
```

### الدوال

| الدالة | الوصف | مثال |
|--------|-------|------|
| `md5(نص)` | تجزئة MD5 | `تشفير.md5("مرحبا")` |
| `sha1(نص)` | تجزئة SHA1 | `تشفير.sha1("مرحبا")` |
| `sha256(نص)` | تجزئة SHA256 | `تشفير.sha256("مرحبا")` |
| `تشفير64(نص)` | تشفير Base64 | `تشفير.تشفير64("مرحبا")` |
| `فك64(نص)` | فك Base64 | `تشفير.فك64("ZW5jb2RlZA==")` |
| `XOR(نص، مفتاح)` | تشفير XOR | `تشفير.XOR("مرحبا"، "مفتاح")` |
| `قيصر(نص، إزاحة)` | تشفير قيصر | `تشفير.قيصر("ABC"، 3)` → `"DEF"` |
| `نص_عشوائي(طول)` | نص عشوائي | `تشفير.نص_عشوائي(16)` |
| `معرّف_فريد()` | UUID | `تشفير.معرّف_فريد()` |
| `HMAC(مفتاح، نص)` | HMAC | `تشفير.HMAC("مفتاح"، "نص")` |

---

## 🔍 مكتبة التعابير النمطية

### الاستيراد
```wisam
استورد تعابير
```

### الدوال

| الدالة | الوصف | مثال |
|--------|-------|------|
| `يطابق(نمط، نص)` | التحقق من التطابق | `تعابير.يطابق("^[أ-ي]+$"، "مرحبا")` |
| `ابحث(نمط، نص)` | البحث عن أول تطابق | `تعابير.ابحث("[0-9]+"، "عمر 25")` |
| `استبدل(نمط، بديل، نص)` | استبدال | `تعابير.استبدل("[0-9]"، "#"، "عمر 25")` |
| `قسّم(نمط، نص)` | التقسيم | `تعابير.قسّم("،"، "أ،ب،ت")` |
| `اعثر_على_الكل(نمط، نص)` | كل التطابقات | `تعابير.اعثر_على_الكل("[0-9]+"، "1 و 2 و 3")` |
| `مجموعات(نمط، نص)` | استخراج مجموعات | `تعابير.مجموعات("(\\d+)-(\\d+)", "10-20")` |
| `نمط_بريد()` | نمط البريد | `تعابير.نمط_بريد()` |
| `نمط_رابط()` | نمط الرابط | `تعابير.نمط_رابط()` |
| `نمط_هاتف()` | نمط الهاتف | `تعابير.نمط_هاتف()` |
| `نمط_IP()` | نمط IP | `تعابير.نمط_IP()` |

---

## ⚙️ مكتبة النظام

### الاستيراد
```wisam
استورد نظام
```

### الدوال

| الدالة | الوصف | مثال |
|--------|-------|------|
| `نفّذ(أمر)` | تنفيذ أمر | `نظام.نفّذ("ls -la")` |
| `التقط(أمر)` | تنفيذ والتقاط | `نظام.التقط("echo مرحبا")` |
| `احصل_متغير(اسم)` | متغير بيئة | `نظام.احصل_متغير("HOME")` |
| `عيّن_متغير(اسم، قيمة)` | تعيين متغير | `نظام.عيّن_متغير("X"، "1")` |
| `معرف_العملية()` | PID | `نظام.معرف_العملية()` |
| `معرف_الأب()` | PPID | `نظام.معرف_الأب()` |
| `معرف_المستخدم()` | UID | `نظام.معرف_المستخدم()` |
| `اسم_المستخدم()` | اسم المستخدم | `نظام.اسم_المستخدم()` |
| `المجلد_الرئيسي()` | HOME | `نظام.المجلد_الرئيسي()` |
| `مسار_التنفيذ()` | PATH | `نظام.مسار_التنفيذ()` |
| `توقف(ثواني)` | إيقاف مؤقت | `نظام.توقف(5)` |
| `توقف_مللي(مللي)` | إيقاف مللي | `نظام.توقف_مللي(500)` |
| `اخرج(رمز)` | الخروج | `نظام.اخرج(0)` |
| `أرسل_إشارة(PID، إشارة)` | kill | `نظام.أرسل_إشارة(1234، 9)` |
| `انشق()` | fork | `نظام.انشق()` |
| `انتظر()` | wait | `نظام.انتظر()` |
| `شغّل(برنامج، ...)` | exec | `نظام.شغّل("ls"، "-la")` |
| `معلومات()` | معلومات النظام | `نظام.معلومات()` |

---

## 💾 مكتبة المخزن

### الاستيراد
```wisam
استورد مخزن
```

### الدوال

| الدالة | الوصف | مثال |
|--------|-------|------|
| `أنشئ(اسم)` | إنشاء مخزن | `مخزن.أنشئ("بيانات")` |
| `أضف(اسم، مفتاح، قيمة)` | إضافة بيانات | `مخزن.أضف("بيانات"، "مفتاح"، "قيمة")` |
| `اقرأ(اسم، مفتاح)` | قراءة بيانات | `مخزن.اقرأ("بيانات"، "مفتاح")` |
| `احذف(اسم، مفتاح)` | حذف بيانات | `مخزن.احذف("بيانات"، "مفتاح")` |
| `قائمة(اسم)` | قائمة المفاتيح | `مخزن.قائمة("بيانات")` |
| `امسح(اسم)` | مسح المخزن | `مخزن.امسح("بيانات")` |
| `أزل(اسم)` | حذف المخزن | `مخزن.أزل("بيانات")` |

---

## 🧠 مكتبة الذكاء

### الاستيراد
```wisam
استورد ذكاء
```

### الدوال

| الدالة | الوصف | مثال |
|--------|-------|------|
| `أنشئ_عقل(اسم)` | إنشاء عقل | `ذكاء.أنشئ_عقل("مساعد")` |
| `علّم(عقل، نمط، رد)` | تعليم العقل | `ذكاء.علّم("مساعد"، "مرحبا"، "أهلاً")` |
| `اسأل(عقل، سؤال)` | سؤال العقل | `ذكاء.اسأل("مساعد"، "مرحبا")` |
| `احفظ_عقل(عقل، مسار)` | حفظ العقل | `ذكاء.احفظ_عقل("مساعد"، "mind.wsm")` |
| `حمّل_عقل(اسم، مسار)` | تحميل العقل | `ذكاء.حمّل_عقل("مساعد"، "mind.wsm")` |
| `أنشئ_منظومة(اسم)` | إنشاء منظومة | `ذكاء.أنشئ_منظومة("كيان")` |
| `أضف_عقل(منظومة، عقل)` | إضافة عقل | `ذكاء.أضف_عقل("كيان"، "مساعد")` |
| `استجب(منظومة، إدخال)` | استجابة | `ذكاء.استجب("كيان"، "مرحبا")` |
| `أنشئ_شبكة(اسم)` | إنشاء شبكة | `ذكاء.أنشئ_شبكة("شبكة1")` |
| `درب(شبكة، بيانات)` | تدريب | `ذكاء.درب("شبكة1"، "data.csv")` |
| `تنبؤ(شبكة، إدخال)` | تنبؤ | `ذكاء.تنبؤ("شبكة1"، [1،2،3])` |
| `استخدم_GPU(شبكة)` | تفعيل GPU | `ذكاء.استخدم_GPU("شبكة1")` |

---

## 🎬 مكتبة الوسائط

### الاستيراد
```wisam
استورد وسائط
```

### الدوال

| الدالة | الوصف | مثال |
|--------|-------|------|
| `اعرض_صورة(مسار)` | عرض صورة | `وسائط.اعرض_صورة("image.png")` |
| `شغّل_فيديو(مسار)` | تشغيل فيديو | `وسائط.شغّل_فيديو("video.mp4")` |
| `شغّل_صوت(مسار)` | تشغيل صوت | `وسائط.شغّل_صوت("audio.mp3")` |
| `أوقف()` | إيقاف | `وسائط.أوقف()` |
| `سجّل_صوت(مسار، مدة)` | تسجيل صوت | `وسائط.سجّل_صوت("rec.wav"، 5)` |
| `التقط_صورة(مسار)` | التقاط صورة | `وسائط.التقط_صورة("capture.jpg")` |
| `معلومات(مسار)` | معلومات الملف | `وسائط.معلومات("video.mp4")` |

---

## 🖼️ مكتبة الواجهات

### الاستيراد
```wisam
استورد واجهات
```

### الدوال

| الدالة | الوصف | مثال |
|--------|-------|------|
| `أنشئ_نافذة(عنوان)` | إنشاء نافذة | `واجهات.أنشئ_نافذة("تطبيقي")` |
| `أضف_زر(نص)` | إضافة زر | `واجهات.أضف_زر("اضغطني")` |
| `أضف_نص(نص)` | إضافة نص | `واجهات.أضف_نص("مرحبا")` |
| `أضف_إدخال(placeholder)` | إضافة إدخال | `واجهات.أضف_إدخال("اكتب هنا")` |
| `أضف_صورة(مسار)` | إضافة صورة | `واجهات.أضف_صورة("image.png")` |
| `اعرض()` | عرض النافذة | `واجهات.اعرض()` |
| `أخفِ()` | إخفاء النافذة | `واجهات.أخفِ()` |
| `أغلق()` | إغلاق النافذة | `واجهات.أغلق()` |
| `عيّن_العنوان(عنوان)` | تعيين العنوان | `واجهات.عيّن_العنوان("عنوان جديد")` |
| `احصل_إدخال(معرف)` | قراءة إدخال | `واجهات.احصل_إدخال("حقل1")` |
| `عيّن_نص(معرف، نص)` | تعيين نص | `واجهات.عيّن_نص("label1"، "نص جديد")` |
| `شغّل_الحلقة()` | تشغيل الحلقة | `واجهات.شغّل_الحلقة()` |

---

## 🌐 مكتبة الشبكة

### الاستيراد
```wisam
استورد شبكة
```

### الدوال

| الدالة | الوصف | مثال |
|--------|-------|------|
| `http_get(رابط)` | GET request | `شبكة.http_get("https://api.com")` |
| `http_post(رابط، بيانات)` | POST request | `شبكة.http_post("https://api.com"، "{}")` |
| `حمّل(رابط، مسار)` | تحميل ملف | `شبكة.حمّل("https://..."، "file.zip")` |
| `أنشئ_خادم(منفذ)` | إنشاء خادم | `شبكة.أنشئ_خادم(8080)` |
| `احصل_IP()` | IP الحالي | `شبكة.احصل_IP()` |
| `فحص_منفذ(منفذ)` | فحص منفذ | `شبكة.فحص_منفذ(80)` |
| `ترميز_url(نص)` | ترميز URL | `شبكة.ترميز_url("hello world")` |
| `فك_ترميز_url(نص)` | فك ترميز | `شبكة.فك_ترميز_url("hello%20world")` |
| `أرسل_بريد(إلى، من، موضوع، محتوى)` | إرسال بريد | `شبكة.أرسل_بريد("a@b.com"، "b@c.com"، "مرحبا"، "نص")` |

---

## ℹ️ مكتبة الميتا

### الاستيراد
```wisam
استورد ميتا
```

### الدوال

| الدالة | الوصف | مثال |
|--------|-------|------|
| `اسم_البرنامج()` | اسم البرنامج | `ميتا.اسم_البرنامج()` |
| `نظام_التشغيل()` | نظام التشغيل | `ميتا.نظام_التشغيل()` |
| `استخدام_الذاكرة()` | استخدام الذاكرة | `ميتا.استخدام_الذاكرة()` |
| `معرف_العملية()` | PID | `ميتا.معرف_العملية()` |
| `معرف_الأب()` | PPID | `ميتا.معرف_الأب()` |
| `معلومات_النظام()` | معلومات النظام | `ميتا.معلومات_النظام()` |
| `وقت_التشغيل()` | وقت التشغيل | `ميتا.وقت_التشغيل()` |
| `إصدار_وسام()` | إصدار وسام | `ميتا.إصدار_وسام()` |
| `المترجم()` | معلومات المترجم | `ميتا.المترجم()` |
| `عدد_المعالجات()` | عدد CPUs | `ميتا.عدد_المعالجات()` |
| `اسم_المستخدم()` | اسم المستخدم | `ميتا.اسم_المستخدم()` |
| `المجلد_الحالي()` | المجلد الحالي | `ميتا.المجلد_الحالي()` |

---

<div align="center">

**📧 للتواصل:** [droy.ree@gmail.com](mailto:droy.ree@gmail.com)

</div>
